# Runtime dependencies (abstract). Authoritative source is pyproject.toml;
# this file is kept for `pip install -r`. For pinned versions see requirements.lock.
networkx>=3.2,<4
pyyaml>=6.0,<7
requests>=2.31,<3      # NVD / CISA / MITRE access
matplotlib>=3.8,<4     # figures
numpy>=1.26,<3         # exposure heatmap

# dev tooling
pytest>=9.0.3,<10      # 9.0.3 fixes CVE-2025-71176
ruff>=0.5,<1
mypy>=1.10,<2
pip-audit>=2.7,<3
